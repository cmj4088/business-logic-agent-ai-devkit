# 种子数据插入

## 测试目的
直接向 SQLite 数据库插入测试数据，使审核页面、产出物页面有内容可展示，无需手动创建项目。

## 运行方式
```bash
# 确保数据库已初始化（后端至少启动过一次）
python demo/seed-data/seed_data.py
```

## 预期结果
- 向 ipd_agent.db 插入测试用户、项目、活动、审核记录等
- 控制台输出插入成功的记录数
- 登录后可看到测试数据
